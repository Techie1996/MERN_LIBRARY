{
  "scripts": {
          "start": "node backend/server.js",
  "build": "cd frontend && npm run build && ",
  "postbuild": "cd frontend && npm install && npm run build && icacls node_modules\\react-scripts\\bin\\react-scripts.js /grant Users:(F)"
},
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.3"
  }
}
