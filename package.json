{
  "scripts": {
          "start": "node backend/server.js",
    "build": "npm install && cd frontend && npm install && npm run build"
},
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.3"
  }   
}
